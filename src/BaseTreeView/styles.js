import { makeStyles } from "@material-ui/core";

export const useBaseTreeViewStyles = makeStyles(() => ({
  treeViewRoot: {
    height: 216,
    flexGrow: 1,
    maxWidth: 400
  },
  listIcon: {
    width: "auto",

    fontSize: "16px"
  }
}));
