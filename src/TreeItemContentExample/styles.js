import { makeStyles } from "@material-ui/core";

export const useTreeItemStyles = makeStyles(() => ({
  treeItemRoot: {
    display: "flex",
    alignItems: "center"
  },
  treeItemIcon: {
    marginRight: "8px"
  }
}));
